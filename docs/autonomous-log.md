# Autonomous Work Log

> A plain, chronological record of what the background loop ships while the user is away.
> One line per item: `- YYYY-MM-DD — what changed (commit)`. Git history is the source of
> truth; this is the human-scannable summary. The roadmap lives in `docs/product-roadmap.md`.

## 2026-06-04 — session kickoff (foreground, before the loop took over)
- 2026-06-04 — eslint jest-globals fix (lint 559→196) + CI workflow + track babel/eslint configs (f028e28)
- 2026-06-04 — trip lifecycle: trips open on the phase-correct day, no stale "Day 2" (5494ea3)
- 2026-06-04 — roadmap: review score, GTM, ship timeline, monetization, lifecycle + notifications (13028ce, 4cf7111)

## Background loop entries (appended by each autonomous run)
<!-- newest at the bottom -->
- 2026-06-04 — trip status pill + phase-aware day chips (f704b0d) ✓ verified on device ("📅 In 8 days" renders for the upcoming Wisconsin trip)
- 2026-06-04 — 2-opt route refinement in scheduleDay/autoArrange (engine, unit-tested)
- 2026-06-04 — "Today" lens: now/next banner + jump-to-today for active trips (engine+UI; active visuals pending an active-dated trip) 
- 2026-06-04 — tiring_day Trip-Check tip (few-but-long day, hours-on-your-feet) (dfd1449)
- 2026-06-04 — fixed 3 real lint-surfaced bugs: Discover reverseGeocode crash + 2 dead-style dupe keys (83b58de)
- 2026-06-04 — feature buckets doc: all forward work organized before/during/after the trip (6f32871)
- 2026-06-04 — DURING: per-family running tally on the Today view (calcFamilyBalances, 4 tests) (21f73fc); status pill re-verified on device, active-state render still pending an active-dated trip
- 2026-06-04 — BEFORE: "Plan my day" now makes a day TIME-feasible (comfortPass — clears the meal→far-sight travel leg scheduleDay's per-type passes miss) + a PREVIEW-DIFF the user Applies/Discards (panel-reviewed, user chose preview). ✓ verified on device; device testing caught & fixed a midnight-wrap bug (29:50→05:50) → now honest "won't fit in one day". 156 tests
- 2026-06-04 — "respect real time / lock what's booked / warn what's risky" (panel-reviewed, user chose Lock+check-in/out): timeLocked + 🔒 pin (lock-on-exact-time, engine anchors it) ✓ device-verified · real hotel check-in/out times the planner respects (default 3pm/11am, store v3→v4) ✓ device-verified · calm "check out by X" tip. 163 tests
- 2026-06-04 — last-day "Heading home?" one-tap return-journey draft (panel-reviewed; user chose one-tap-draft over silent auto-create): returnJourneyDraft pre-fills ONLY known facts (home origin, mirrored arrival mode), never a cost (protects the split), find-or-update so no duplicate; [Add return]/[Not now] card on the last day; check-out stays derived. ✓ device-verified end-to-end. 169 tests
- 2026-06-04 — return draft made OPEN-JAW aware (panel-reviewed): departs from where the trip ENDS (tripEndLocation = last night's lodging → last stop), not where it arrived (fly LA→drive SD→fly home from SD); loop guard suppresses when the trip ends back near home; round-trip-aware copy (flight = already-paid record, drive = cost editable). 171 tests
- 2026-06-04 — end-anchored last-day routing: pathCost/routeOrder/scheduleDay take an optional endAnchor; Plan my day fixes the last day's FINISH at the return's departure point (wake → stops → airport), so stops flow toward where you leave from. No endAnchor = identical to before. 174 tests; device smoke-tested (no crash)
- 2026-06-04 — "Play My Trip" (panel-reviewed across many seats): a cinematic in-app montage of the trip from cached place photos — ▶ in the sticky bar, a PLANNER MOTIVATOR (sharing later). v1 = pure testable buildTripFilm: reward-led, IMAGE-BACKED motivational bookends, ONE forward nudge (from validateTrip), ≤2 double-duty teach lines (the moat, reward-framed), state-adaptive (trailer/building/victory), never ends on a gap. Fixed the black-void bug (warm base + image-error fallback). In-app only, no music yet, no export. 180 tests; device-verified bookends+photos+close
- 2026-06-04 — Play My Trip polish: no blank gradient cards — day marker folded onto its first photo, progress/nudge cards ride a dimmed photo (e25c55b). MUSIC: an ORIGINAL synthesized ambient bed (scripts/gen-music.js → assets/playtrip-bed.wav, zero rights risk — no sample/track), looped via expo-audio (SDK54, Expo Go) at a quiet fade-in, in-app only (c89367b). 181 tests; device-verified clean reload + plays
- 2026-06-04 — lint cleanup (path-to-10 "lint/CI" win): eliminated the ENTIRE react/no-unescaped-entities category — 39 display-text apostrophes/quotes escaped across 13 screens/modals (line-scoped, JSX expressions untouched). Zero behavior change (pure text), 214 tests stay green; eslint 212→173 problems (111→72 errors). Deferred the rule-registry + store-slice refactors (§9.4): too large to do "small" + only partially snapshot-covered → better with the user available
- 2026-06-05 — lint cleanup (§9.1 highest-ROI / path-to-10): removed dead code across 14 files — unused imports + pure dead local vars (FamilyBudgetAgent memberShare/roomShare/orphaned expenseGroups, pipeline nights, exportPlan CAT_ICONS/now/safeName, plannerAPI SD/SFO imports + context + isArrival, places/PlayTripModal unused catch bindings → optional-catch, + 8 unused imports). Zero behavior change (no functions/handlers/setters/tests touched; react-compiler ref/setState/impure diagnostics left for device review); 325 tests + 25 snapshots green; eslint 175→148 problems (−27 warnings, 0 new)

- 2026-06-05 — chore(lint): dropped 3 unused imports from ItineraryScreen (activityIcons / summariseWarnings / scheduleDay); behavior-neutral, screen lint 10→7, 422 tests green (68e5b66). DEFERRED roadmap item (a) close-time-first ordering — it's a planning-quality tradeoff (drive farther to catch an early-closing venue, or not?) = a product/policy decision, so flagged for the owner rather than decided unattended.
- 2026-06-06 — §9.6 coverage: direct unit tests for costs.js (the split/settlement MOAT) — 26 store-free tests covering every pure share fn incl. empty-family no-leak, family/individual even+uneven, unbalanced-custom fallback, itinerary rollups, balances/settlement. costs.js 82→99% stmts / 74→89% branch / 72→100% funcs. Tests only, zero behavior change; 483 green (67bfd93). (Loop note: roadmap deterministic queue is essentially done — close-first+2-opt, rule-registry, store-split, status-pill, tiring_day all shipped; remaining items are UI-wiring/notifications/backend or store-coverage, which is a larger multi-run effort.)

## 2026-06-05 — refactoring session (autonomous lead, panel-guided)
Owner handed the lead for ~8h to refactor, going with the panel's recommendation, committing
frequently for review. Safe mode (panel consensus): extract PURE logic from the big screens
into unit-tested utils (jest-provable); NO JSX/component extraction or react-native-testing-library
setup (unverifiable without a device; reanimated-v4 mocking too fragile to stand up unattended).
Each increment: jest green + count stable, snapshots byte-identical (never -u), lint not worse,
diff only intended files. Target: ItineraryScreen.js (3022 lines) + DiscoverModal/SplitwiseScreen.
- 2026-06-05 — compile safety net: a babel-transform test over every screen + modal, so a
  syntax/JSX break in a file jest never imports now turns a test red. 330→355 tests.
- 2026-06-05 — ItineraryScreen dedup: removed the local getSlotKey + toMin (byte-identical to
  slots.js getSlotKey/timeToMin → import those instead) and the dead SEV_CHIP palette. ~16 lines
  off the monolith; behavior-identical; 355 tests green (no new lint).
- 2026-06-05 — extracted getDietaryWarning (+ meat/alcohol regexes) → utils/dietary.js with 8
  unit tests (previously 0 coverage on the dietary-warning logic). Screen imports it; byte-identical
  function; 363 tests green.
- 2026-06-05 — extracted generateDayShareText (+ SLOT_RANGES + its slot helpers) → utils/dayShare.js
  with 8 unit tests (was 0 coverage on the WhatsApp/Share text). Dropped now-orphaned imports
  (googleMapsDayShareUrl, APP_NAME) from the screen. ItineraryScreen 3022→2936 lines; 371 tests green.
- 2026-06-05 — extracted computeTripHealth (per-day Trip-Check health for the day pills) →
  utils/tripHealth.js with 7 unit tests (validateTrip mocked for deterministic severities; logic
  had 0 coverage). useMemo body collapsed to a one-line call. ItineraryScreen 2936→2921; 378 tests.
  Session so far: 3022→2921 lines (−101), 5 new tested utils, 330→378 tests, all green.
- 2026-06-05 — extracted tripCheckStatus (the Trip-Check chip's fix/look/building/clear decision +
  conflict/check counts) → utils/tripCheckStatus.js with 8 unit tests (validateTrip mocked; 0→tested).
  Screen keeps only the state→colors/icon/label mapping; dropped the now-unused validateTrip import.
  Behavior-identical chip; 392 tests green; screen lint unchanged (f04423a).
- 2026-06-05 — extracted buildStatusPill (trip status pill: upcoming countdown / active day-of /
  past complete) → utils/tripStatus.js with 6 unit tests (date injected for determinism; 0→tested).
  Dropped the now-unused daysBetweenISO import from the screen. Behavior-identical pill; 398 tests
  green; screen lint unchanged (c5d27c2).
- 2026-06-05 — moved the 10 inline lookup tables (DAY_SLOTS, ACT_ICON, SEV_RANK, PILL_TONE,
  DAY_PILL, HEALTH_DOT, SLOT_MEAL, MEAL_LABEL, NIGHT_PLAN_OPTIONS/META) → utils/itineraryConfig.js
  + 4 invariant tests (slot tiling, slot→meal, night-plan key parity, sev ordering). Pure data,
  behavior-identical (no-undef/no-unused confirm clean); screen −62 lines; 402 tests green (7baa9d6).
- 2026-06-05 — extracted Discover's pure Places helpers (mapPlace, inferActivityType, metersBetween,
  PRICE_TO_COST, isVegFriendly, photoUrl) → utils/discoverPlaces.js + 11 tests (incl. a regression
  lock on the Great-Wolf-Lodge "lodging beats food" classification). Screen imports back mapPlace +
  metersBetween, drops the now-unused compactHours import. −49 lines; 413 tests green (de4b982).
- 2026-06-05 — extracted summariseExpenses (Splitwise display rollup: itinerary/manual,
  included/skipped, included-only totals) → utils/expenses.js + 3 tests. Presentational glue only —
  the split/settlement money math (costs.js) untouched. SplitwiseScreen −8 lines; 416 tests green
  (a854463). EXTRACTION QUEUE EXHAUSTED — next run is the Play My Trip multi-photo feature.
- 2026-06-05 — FEATURE: Play My Trip fuller per-day photo reel — each planned day opens with its
  vibe card then a short reel of that day's OTHER cached act.photo stops (cap 3/day, 10 overall;
  no live fetch; transport skipped; gradient fallback). Pure in tripFilm.js; PlayTripModal
  unchanged; +6 tests; 422 green; snapshots byte-identical (3ed4091).
  SESSION COMPLETE — loop stopped (no further wakeups scheduled).
- 2026-06-05 — ✓ Play My Trip (cached photos + 4 music themes + multi-photo reel) VERIFIED ON
  DEVICE by the owner ("looks good"). The on-device open item from this session is now closed.
- 2026-06-05 — roadmap item (a) close-time-first ordering — DONE, comfort-first per owner's call
  ("keep the trip comfortable, not rushed; add the detour requirement later"). routeOrder now does
  distance routing then a bounded comfortClosePass that pulls earlier-closing stops forward ONLY
  within a 2 km comfort budget (never detours). No-op without close-times → all snapshots unchanged.
  +7 tests; 429 green (8278eaa). The aggressive "detour to catch a closing" mode stays deferred.
- 2026-06-06 — lint cleanup: cleared 6 genuine eslint warnings (90→84, still 0 errors) — unused test import/var, an orphaned PACE_LABELS const, a stale eslint-disable, and documented App.js's intentional gesture-handler double-import. Left all possible-WIP scaffolding (unused store actions/state/handlers) per the don't-delete-scaffolding rule. 510 tests green, snapshots byte-identical (fd76e49). Deterministic priority queue (a–f) now exhausted; remaining roadmap items are UI/device or backend.
- 2026-06-06 — coverage (roadmap §9.6): hardened slots.js (core planner time/slot util) 78.6%→95.5% stmts, 62.7%→84% branch, 100% funcs/lines — direct tests for timeToMin/minToTime, getSlotKey/getSlotDefaultTime/getSlotCount, and the getSmartTime/getSuggestedTime null+full-slot fallbacks. Tests-only, no behavior change; 526 tests green, snapshots byte-identical (ec79446).
- 2026-06-06 — coverage: tripValidator pure helpers — estimateDuration's ship/unknown transport sub-modes (was missing 'ship'=240 + the default branch) and the previously-untested groupWarningsByDay / summariseWarnings (new validatorHelpers.test.js). +6 tests, 539 green, snapshots byte-identical; also gitignored the generated /coverage dir.
- 2026-06-06 — ✓ DEVICE-VERIFIED by the owner ("those work"): the Plan-My-Day route+time reorder feel (e73ad78), honoring the lock on manual moves + the reorder-past-a-locked-stop warning (34f519a, 73e5d86), and the Split-tab swipe-to-resolve on unchecked-item rows (ae6be0c). The on-device open items from the planner/split rework are now closed. Tagged the state: `checkpoint-2026-06-06-planner-split-verified`.
- 2026-06-06 — coverage: hours.js (opening-hours model) 84.2%→90.5% branch, 100% funcs/lines — added dayName (was uncovered) + clock-formatting edges (minutes, 12 AM/12 PM). Tests-only; 543 green, snapshots byte-identical.
- 2026-06-06 — UI (owner invited creative UI; checkpoint tag set first): unified "trip identity" visual language across the three trip surfaces. (1) richer trip cards — status pill + family avatar stack (5e68d21); (2) extracted FamilyStack, used on cards + next-trip spotlight (3d7a405); (3) gradient cover header on the trip screen — brand hero gradient, per-trip emoji tile, countdown pill, frosted chips, tabs on white below (2f42c69). All handlers preserved; compile-net + 543 tests green, eslint 0 errors. ✓ DEVICE-VERIFIED by the owner 2026-06-07 ("Looks Good. Just checked.") — revert tag was checkpoint-2026-06-06-planner-split-verified (not needed).
- 2026-06-07 — UI cont'd (owner: "Continue"): extended the language into the Split tab — per-family proportional SHARE BARS (family-color fill = slice of the trip total) + a celebratory all-settled success card. PURE-VISUAL, costs.js untouched (da6e9fd). New checkpoint tag before this batch: checkpoint-2026-06-07-ui-verified. ⚠️ visual — needs device check. The big screens (Itinerary/People, already well-designed + large) intentionally left for an at-device pass — too risky to redesign blind for the reward.
- 2026-06-07 — coverage: expenses.js branch 73.6%→79.2% (100% stmts/funcs/lines) — activityToExpense empty-members/splitMode/unknown-type branches, unconfirmedSplitItems no-time + unparseable-date edges, withUnconfirmedExcluded pass-through. Tests-only; 548 green, snapshots byte-identical.
- 2026-06-07 — coverage: tripCopy.js (shared-PDF motivational copy) 88%→100% stmts, 57.7%→71.8% branch — dayVibe now exercises every rule-derived category (arrival/travel/packed/foodie/outdoors/culture/relaxed/mixed). Tests-only; 549 green, snapshots byte-identical.
- 2026-06-07 — DESIGN doc (owner-requested, design only): docs/timezone-model.md — how Voyara handles cross-timezone trips. Key call: most itinerary times are FLOATING wall-clock ("7pm wherever you are"), not absolute instants → store local HH:MM + the place's IANA tz NAME (not a numeric delta, which rots across DST); only cross-zone travel legs are true instants (UTC/duration derived, never frozen). Covers the store v5→v6 additive schema + migrate, the gating Hermes Intl spike (decides pure-JS vs library) as impl step 1, tz inference from stop coords, the zone-badge UI, red-eye/jet-lag Trip-Check tips, and a 5-phase rollout (phases 0–1 pure-JS + unit-testable). No code (626dfdf).
- 2026-06-07 — SMART PASTE made "one key away" (owner concluded the text-understanding needs an LLM): itineraryExtract.js — deterministic front-half only (NO keys/network/provider; LLM injected). Documents the extraction CONTRACT; normalizeExtraction() repairs loose LLM JSON → the exact assembler shape (Day-N date synth, bad-type→activity, drop unnamed, mark options, pad times, never throws); extractItinerary(text,{extract}) seam = injected LLM (source:'ai') else rules parser (source:'rules'), resilient. Reuses the whole assembler/gazetteer back-half unchanged. +8 tests incl. e2e AI-JSON→real-trip, 689 green, eslint 0. Smart Paste flag stays OFF (dormant prep) (30f055a).
- 2026-06-07 — Trip Wrapped: per-family spend added to computeTripStats (perFamily [{id,name,color,total}] + topSpender) via the existing calcFamilyExpenseTotal — moat math reused read-only, not re-implemented; guarded for empty trips. +2 tests, 681 green, eslint 0 (c8c7b5c).
- 2026-06-07 — Trip Wrapped recap stats engine (roadmap §11 post-trip recap): utils/tripStats.js computeTripStats + tripStatLines — places, cities, TIME ZONES SPANNED (reuses tzForDay), days/nights, types, flights/drives, busiest day, total shared spend (via summariseExpenses, no money-math touch). Pure + 7 tests, 679 green, eslint 0. UI (recap screen) deferred to a later device fire (a75c9c7).
- 2026-06-07 — FEATURE: Smart Packing List (owner: "be creative, make it award-worthy, autonomous mode"). A deterministic, no-AI engine that REASONS from the trip — beach→swimsuit/sunscreen, hike→boots, theme-park→comfy shoes, red-eye→eye mask, international (diff tz region)→passport/adapter, season→sun vs warm layers, infant→diapers, child→snacks, wheelchair→accessibility kit, clothing scaled by nights — each item with a WHY. utils/packing.js buildPackingList (11 tests); persisted check-state via store v6→v7 (additive `packing` map + togglePackingItem, shape guard updated, +2 tests); PackingModal (progress bar, per-item reason, persisted checkboxes, all-packed celebration) reached from the trip ⋮ menu. 672 tests green, eslint 0. Engine+store fully tested; ⚠️ modal UI needs device check. The "app anticipates your needs" delight that fits the award-worthy bar + the "enjoy the trip, don't worry" vision.
- 2026-06-07 — UI polish (owner: "keep making UI better" while away): conservative, additive touches reusing device-verified patterns — FamilyStack avatar row in the People header (matches Home/Trip cards) (fbe8dc1); warmer Itinerary empty-day state (map emoji + "Let's fill Day N" + hint; empty branch only, zero risk to populated days) (b31c147). Checkpoint tag checkpoint-2026-06-07-pre-ui-polish set first. 659 green, eslint 0. ⚠️ visual — needs device glance. (Held back from blind redesigns of already-polished screens given recent device misses; best ROI now is device-informed polish.)
- 2026-06-07 — SMART PASTE put ON HOLD (owner: "not working" on device). Hid the Home "Paste a plan" button via RELEASE_FLAGS.smartPaste=false (local config, gitignored); all parser/assembler/gazetteer code stays (tested, dormant) for a later revisit + on-device debug. Pivoted to UI polish per owner's direction.
- 2026-06-07 — SMART PASTE: offline gazetteer (29 airports by IATA + 22 cities) → the assembler gives imported flights + known-city stops real coords with NO API (segment-city fallback too), so the timezone features light up on pasted trips; specific POIs still need Places (stage D). Every coord self-validated against its declared IANA zone via tzForCoords (typo guard). +13 tests, 659 green (7d5d48b).
- 2026-06-07 — SMART PASTE MVP (owner: "make paste-AI-plan→trip work; it's the disruptor"; 10h away, full autonomy on safe work). Built the whole deterministic paste-to-trip pipeline (no AI/API): itineraryImport.js assembler (parsed→real trip via createTrip+addActivity: dates→days, slots/explicit→times, type lexicon, Option B→skipped, vague→note; +6 tests) (a3e70cb); PasteImportModal + Home "Paste a plan" button behind RELEASE_FLAGS.smartPaste (config is gitignored so flag is owner-local; undefined→hidden elsewhere) (acca455); parser hardening — markdown bullets/bold + "Day N" headers w/ date synthesis (264febb) + wall-of-text re-segmentation (cc190b4); devSeed → DEMO_SEEDS menu of 4 scenarios (cross-zone happening, Tokyo red-eye, multi-family split, paste-built) (e93a19d). 652 tests, eslint 0. Stage D (Places resolution→coords/hours/cost + tz enrichment) deliberately DEFERRED — API cost, needs owner greenlight + caching. ⚠️ paste UI + demo menu need device check.
- 2026-06-07 — ASSESSMENT + PoC (owner Q while away): "paste a Gemini/ChatGPT itinerary → build a trip deterministically (knowledge-graph + rules, no AI)?" docs/smart-paste-import.md = honest design+assessment: YES to good-DRAFT quality (input is semi-structured; Google Places resolves the hard POI part; KG = grammar + action lexicon + alias/stop-list + Places as live gazetteer; Supabase only Phase-2 for cached/shared KG). Heavy reuse of existing pieces. Effort ~2wk MVP / ~3-5wk v1; entity extraction from prose caps accuracy; included an honest AI-vs-deterministic trade-off. PoC src/utils/itineraryParser.js (pure, no API/UI) parses the owner's EXACT sample → 9 dated days, 3 segments, slots/stops/options, type classify, candidate POIs; +10 tests (637 total), eslint 0. Spike, unwired (16e018e).
- 2026-06-07 — dev: one-tap demo trip for tz verification (owner couldn't easily build a cross-zone trip). __DEV__-only Home button "Demo: ORD↔LAX↔SD" seeds a real 5-day trip via createTrip+addActivity: fly ORD→LAX (Central→Pacific), road-trip LA→San Diego (both Pacific), fly LAX→ORD (Pacific→Central), home Chicago — exercises per-activity zone flags, cross-zone leg labels, same-zone drive (no label), + open-to-now/auto-lock (starts today). Gated by __DEV__ (never ships). +5 season-robust tests (627 total), eslint 0 errors (0e52ab3).
- 2026-06-07 — TIMEZONES Phase 2f (owner chose "finish travel-leg labels" while away): crossZoneLeg helper (pure, 6 tests) → a transport crossing zones returns depart/arrive zone labels + true duration + red-eye/+1-day; eyebrow renders "9:00 AM PDT → 5:00 PM EDT · 5h 🌙 +1 day". Depart zone = before the leg, arrive = destination's (or next differing stop) so it works even when a manual flight has no coords. Pure compute committed (1e2b76c, 622 green); UI render (984314f) ⚠️ needs device check on a cross-zone flight + a red-eye. Phase 2 timezone essentially complete (optional: manual zone picker).
- 2026-06-07 — fix(tz): show the zone flag on EVERY activity (dropped the smart badge suppression). Chicago→Wisconsin is all Central, so the single-zone badge (== home) was suppressed and read blank next to Chicago→Michigan (crosses to Eastern). Per owner, resolveDayZones now returns a label for every timed stop (own location; location-less inherits previous; DST-correct), rendered in each card eyebrow; removed the day-level badge chip. Consistent, never blank. Tests → per-activity contract; 617 green, eslint 0 (a9898f2).
- 2026-06-07 — fix(itinerary): zone (EDT/CDT) labels blank on a 2nd happening trip. Root cause: `day = trip.days[currentDay] || trip.days[0]` fell back to day 0 when currentDay was stale/out-of-range (switching to a trip with fewer days), but the per-day memos (dayZones/pastIds/dayPast/dayWarnings/startAnchor) used the RAW currentDay → computed for the wrong index, so zoneById was empty. Fix: one clamped dayIdx drives the rendered day AND all per-day derivations; an effect re-syncs setCurrentDay so write-handlers target the same day. 618 green, eslint 0 (c9cce2f). ⚠️ device check switching between two happening trips.
- 2026-06-07 — TIMEZONES Phase 2e (owner: live trip should auto-lock the past + Plan-my-day only the remaining time): clock-aware locking. Pure helpers pastActivityIds/isDayInPast/planFloorMin (TZ-aware, each stop in its OWN zone; _activityZones extracted as the shared source of truth with resolveDayZones) (2973aaf). scheduleDay gains earliestMin floor — a [0,now] occupied block so free stops never schedule into the past while locked/past stops keep their earlier times (6c3a730). UI: a passed stop is auto-locked (guardMove/guardSwap → "🕒 already happened — move anyway?", eyebrow 🕒); Plan-my-day disabled on past days / finished trips; on the current day it pins past stops + floors to now, stripping the derived locks before write so clock-state isn't persisted (849f554). Engine+helpers unit-tested, 618 green, eslint 0 errors. ⚠️ UI needs device check on a trip dated to include today.
- 2026-06-07 — autonomous: timezone_shift Trip-Check info tip — a day whose located stops cross a UTC offset (Chicago→NY drive, east/west flight) gets a soft "You cross a time zone — clocks go forward/back Nh" tip. Pure+offline (tzForCoords+offsetMinutes at the day's date → DST-correct); no-net-shift stays silent; distinct from multi_city_day. Surfaces via existing Trip Check rendering (no new UI). No-op on all golden fixtures (GMT coords) → snapshot byte-identical; +4 tests, 610 green (d5bbe42).
- 2026-06-07 — TIMEZONES Phase 2d (owner: "show at activity level … misc manual w/o location uses the previous TZ in the ordered trip"; chose Smart display): helpers.resolveDayZones(trip,i) — each timed activity takes its OWN location's zone (tzForCoords); a location-less activity inherits the previous activity's zone in trip time-order (carries across days). Smart display: one-zone day → single 🕘 badge; zone-SPANNING travel day (e.g. Chicago→Traverse City CST→EST) → no badge, each activity's time tagged with its own zone (9:00 AM CDT … 2:00 PM EDT). ActivityCard gains zoneLabel in the eyebrow. DST-correct per date; offset-based so equal-offset zones don't split a day. +6 tests (606); eslint 0 errors (9fadb02). ✓ owner confirmed the day badge works on device. Still TODO: travel-leg arrival labels + optional manual picker.
- 2026-06-07 — TIMEZONES Phase 2c (owner: "the screen looks the same" → chose auto-infer-from-stops): tzForDay now derives a day's zone from its first LOCATED stop's coords (offline tzForCoords) instead of relying on defaultTz/origin — so a real cross-zone trip lights up automatically even when defaultTz=home (the origin=home case that made the badge invisible), and multi-city trips vary by day. Order: day.tz override → inferred-from-stops → defaultTz → homeTz → device; travel day takes first stop's zone. Backward-compatible (no located stop → old fallback). +5 tests (601); eslint 0 errors (187dde4). NOW TESTABLE: a trip with stops in a different zone than the phone (even domestic, e.g. Central phone + San Diego/Pacific stops) shows the badge. Still TODO: travel-leg arrival labels + optional manual picker.
- 2026-06-07 — TIMEZONES Phase 2b (owner: "keep going" + asked about DST): per-day zone badge — helpers.dayZoneLabel(trip,i) shows a 🕘 EDT/GMT+9 chip on the itinerary day header ONLY when the day's offset differs from home (abroad/cross-zone); single-zone domestic trips stay clean. Compares OFFSETS not names; uses the day's own date so it's DST-correct (test proves EDT in summer / EST in winter, same zone — which answers the owner's DST question: tz-lookup only gives the zone NAME offline, Intl/ICU computes the DST offset per-date). +4 tests (597); eslint 0 errors (561f51e). ⚠️ needs device. Next: travel-leg arrival labels (needs Phase 2c per-day tz inference from stop coords).
- 2026-06-07 — TIMEZONES Phase 2a (owner: "move to phase 2 … when the user opens a Happening trip it should take them to destination day - timeslot activity"): open-to-now. setCurrentTrip now lands on the DESTINATION's current day (tz.zonedNowDate(trip.defaultTz) — Tokyo can be a calendar day ahead of the phone) instead of always Day 1 (this finally wires the long-dead defaultDayFor); TripScreen scrolls to that day's now/next activity via openFocusFor + the existing highlight. New pure pieces: tz.zonedNowDate/zonedNowMinutes, helpers.openFocusFor(trip,nowMs)→{dayIndex,activityId} (reuses defaultDayFor+nowNextOf), all clock-injected. +14 tests (593); eslint 0 errors; degrades gracefully w/o Intl tz (00b41ad). ⚠️ needs device. Still TODO Phase 2: zone badges (7:00 PM PDT) + travel-leg arrival labels.
- 2026-06-07 — TIMEZONES Phase 0+1 (owner: "lets move on to timezone aware change"; design in docs/timezone-model.md): (0) engine src/utils/tz.js — pure, zero-dep, Intl-based; offsetMinutes/zonedWallToUtcMs/tzAbbr/zoneShortLabel/crossZoneLegMinutes/formatGmtOffset/deviceTz + tzSupported() runtime feature-detection (the Hermes spike, baked in so the app degrades gracefully); App.js logs the spike once in __DEV__ (a190a72). (1a) owner chose the OFFLINE table → added tz-lookup (pure-JS ~1MB, lazy-required) + tzForCoords with guards (533ba7b). (1b) store v5→v6: trips gain homeTz+defaultTz, createTrip infers defaultTz from origin coords (else device), updateTrip syncs on origin change, migrate backfills to device zone (additive); tzForDay(trip,i) = the one resolution rule; shape guard updated (1011692). 585 tests green; eslint 0 errors; pure-JS, zero UI yet. ⚠️ NEEDS DEVICE: confirm the [tz spike] log (true/false) + that the v6 migration rehydrates existing trips cleanly. Next: Phase 2 UI zone badges.
- 2026-06-07 — ✓ DEVICE-VERIFIED by the owner ("looks good"): the Split-tab By-Group default + Add-Activity Total cost default (673134a), the chronological itinerary-expense ordering (4a1a8c2), and the resolve-affordance-for-every-unchecked-item fix (9a4edbc). Existing trips keep their saved split mode by design (only new trips default to Group).
- 2026-06-07 — defaults (owner-requested): Split tab defaults to By Group (createTrip splitMode 'individual'→'family' + screen fallback; existing trips keep their saved mode) and Add Activity cost defaults NEW activities to 'total' (planner enters the price, Split tab divides). costPerPerson stays the derived source of truth (Invariant #3) so costs.js + the activity→expense path are untouched; updated the createTrip default assertion + made the per-person settlement test explicit. 559 green (673134a).
- 2026-06-07 — fix(split): list itinerary expenses in itinerary order (day→time) via new pure itineraryExpenseOrder(trip) — raw insertion order scattered the flagged/unflagged rows (the real source of the confusion). Display-only sort; summariseExpenses + money math untouched; guarded the Number('')===0 midnight trap. +3 tests, 559 (4a1a8c2).
- 2026-06-07 — split BUG + 2 enhancements (owner-reported, device session): (1) BUG fix — with 3 itinerary expenses only the first+last showed "Tap to resolve", the middle didn't despite being unchecked. Root cause: the affordance was gated on the TIME-AWARE unconfirmedSplitItems (needs end-time-past AND activity-found-unchecked), so a not-yet-past item / an ORPHANED link (activity edited away) / a DUPLICATE link (linked-Map keeps one expense per activityId) got no affordance. Fix: new pure pendingSplitItems(trip) drives the affordance per-expense with NO time gate; the money auto-exclusion stays time-gated + deterministic (untouched); copy now splits "⚠️ Not counted" (past, excluded) vs "○ Not checked off yet" (still counted). +7 tests, 556 total (9a4edbc). (2) the resolve sheet's "Open in itinerary" → "Go to <day label>" (e.g. Go to Day 2). (3) PINNED the Itinerary day-switcher bar (stickyHeaderIndices) so days are reachable without scrolling back to top on the now-taller photo days; scroll-to-highlight offset 80→150 to clear the bar (4c882af). ⚠️ all UI needs device check.
- 2026-06-07 — UI: ITINERARY DAY REDESIGN (owner picked "Timeline + photos + Stay card" via AskUserQuestion previews; checkpoint-2026-06-07-pre-timeline tagged first for instant revert). Three increments, each its own commit, each compile-net + 549 tests + 27 snapshots green, eslint 0 errors: (1) chronological TIMELINE day view — one time-sorted list with SOFT time-of-day dividers replacing the 4 always-on Morning/Afternoon/Evening/Night sections; travel connectors now chain uniformly across the day; all behaviors preserved (origin/wake leg, move up/down via reorderSlotActivities on the full-day order, lock guards, scroll-to-highlight refs, Doesn't-fit tray, checkout banner, lodging chip); removed dead slot-collapse state (2d3e5e5). (2) PHOTO-FORWARD cards — a live card's place photo is now a full-bleed 150px hero (was a 56px corner thumb); actCard became a column (hero + content row); Book/Website tap target + labelled badge moved onto the hero (fc2c97b). (3) tap-to-BOOK on tonight's lodging chip — 🛏 Book › opens Booking.com (revenue) on every covered night, no duplication of the check-in hero card (f32cad8). ⚠️ ALL UI — needs on-device check. Empty-day onboarding slot template kept; People/Itinerary-internals beyond the day view still deferred.
- 2026-06-08 — autonomous safe-lint batch (roadmap item f): priorities a/b/c/d/e already shipped (2-opt+close-time routeOrder in autoArrange; rulePacked+ruleTiringDay in the validator; trip status pill + phase day chips device-verified; rule-registry + store-slice split). Did the next qualifying item — removed an unused `radius` import (PackingModal) and a stale `global-require` eslint-disable (aiExtract.test); both behavior-neutral. Deliberately LEFT the state-setters/handlers/store-destructures in DiscoverModal/HomeScreen/AgenticPlannerModal (possible scaffolding + device-critical UI). 101→99 eslint warnings, 0 errors; 709 tests + 27 snapshots byte-identical (6ea8e3a).
- 2026-06-08 — autonomous knowledge-base expansion: roadmap (a–e) all shipped and (f) had no safe items left (only `resolveCity` in plannerAPI — a scaffolding fn+CITY_ALIASES table, left per AGENTS.md). Pivoted to the owner's standing direction ("keep expanding the knowledge / make the rule engine smarter") — fully autonomous-safe (deterministic, jest-validated, reversible, no decision): +41 gazetteer cities (US leisure, Europe, MidEast/Africa, Asia, Americas) so imported stops in them geocode (timezone + routing light up). Each coord tz-validated by gazetteer.test.js (Jackson Hole declared America/Boise per tz-lookup's polygon). 750 tests + 27 snapshots green, 0 eslint errors (6b107fb).
- 2026-06-08 — autonomous (owner: "go autonomous, your suggestions make sense"): two approved safe items. (1) PDF export RE-ENTRANCY GUARD — the owner saw export crash once then work (a double-tap race: two printToFileAsync + two share sheets, which iOS rejects). All three exports (trip/day/settlement) now run through a shared `runExclusive` in-flight flag; a second tap while one is generating is ignored. Extracted as a testable helper (no native mocks): proves exclusion + lock-releases-on-throw + no stranded flag. 758 tests green (c2ca90c). (2) docs/phase-2-backend.md — the Supabase design doc capturing the TWO-LAYER knowledge base (open data = GeoNames/OSM/Foursquare/Wikidata, legally storable + compounding; Google as the live fill-in within ToS caching — Place IDs storable forever, details ≤30 days, NO substitute DB), the schema, edge functions (keys server-side), and an incremental 2a→2c migration. Triggered by the owner's Q on legal risk of a shared Google cache — answer: the naive "cache Google forever" version violates the Maps Platform Terms, so the compounding layer must be open-data. Needs owner go-ahead (external accounts/cost) — NOT autonomous to execute. Also this session (interactive): Wikimedia Commons added to the free-first photo resolver (9ba7ab0).

## 2026-06-04 — test-user bug fixes (user driving)
- 2026-06-04 — Bug1: 24/7 places (bridge/lighthouse) no longer show "Closed" — Google's no-close period was recorded Sunday-only; compactHours now expands 24/7 to all 7 days + handles cross-midnight; store v4→v5 migration repairs existing trips. ✓ device-verified (19b84aa)
- 2026-06-04 — Bug2: typed Discover search is literal (no dietary-bias pollution) + unclamped (a named place outside the viewport isn't dropped); address autocomplete now surfaces street addresses (Photon no-name filter fixed); "Search without filters" override in the empty state. ✓ device-verified search (1ae0468, 59bcbf2, e62236b)
- 2026-06-04 — multi-day travel-day fix (panel-designed, QA-tested): first stop now clears the drive from where the day STARTS (home Day 1 / hotel later) — fixes "359 km to your first stop at 08:00"; travelLeg speed is distance-aware (urban/mixed/highway, was a flat 26 km/h → 360 km read as 14h); Trip Check "first stop is early for the drive / Day 1 looks like a travel day" tip (drive vs flight wording) + hotel-check-in nudge; comfortPass caps the push at ~15:00 for flight-distance anchors. ✓ device-verified (227 min travel time, the travel-day tip renders). 201 tests (aca0b2a, 8348336, 25f18ab)
- 2026-06-04 — Bug (test user): cards showed a red "Closed" for a venue closed on the SELECTED day (Boardman River Nature Center — open Tue–Fri 10–4, Day 2 landed on a closed day) — reads like a live status, says nothing about when to go. Owner: "we need hours of operation, not live status." New weeklyHoursLabel summarises hours into day ranges ("Tue–Fri 10 AM–4 PM"); cards show that day's hours when open, the weekly hours when closed that day, neutral clock — no red status (scheduling-while-closed stays a Trip Check concern). Applied to the itinerary card + both Discover cards. ✓ device-verified (Mission Point Lighthouse "Closed" → "Mon 10 AM–5 PM, Wed–Sun 10 AM–5 PM"). +6 tests, 213 total (25b4b54)
- 2026-06-04 — Discover shows ALL places ranked by quality only; traveler-profile filtering is now a planner OPT-IN (c43e9f9). Ranks by qualityScore (rating + damped popularity), opens unfiltered (chips no longer auto-seeded from the profile), dropped the scorePlace group-fit hard-exclusion that could hide a place entirely. Why: a tester searched Great Wolf Lodge, never saw it, lost interest. ✓ device-verified (chips open unselected, 100 places, ranked by quality+footfall)
- 2026-06-04 — Bug (test user): "Great Wolf Lodge" typed search returned a blank list in Traverse City (e1b7595). Confirmed against the live Places API that Google DOES return it — it was hidden downstream: (1) inferActivityType checked food before lodging, so the resort (types include restaurant+food AND resort_hotel+lodging) was filed as Eat and the Stay-only layer filter dropped it → lodging now wins; (2) typed search was still layer-filtered → typed search is now literal (shows every match; layers/dietary chips hidden during a typed search, they only narrow browse). ✓ device-verified ("Great Wolf Lodge" → 1 result, "Book rooms" CTA = now classified stay)
- 2026-06-04 — QA code-audit pass (two agents read the actual Plan-my-day + Discover source against the QA test matrices) → fixed ALL 7 confirmed bugs (3 HIGH / 2 MED / 2 LOW). Plan my day: (80c3541) "HH:MM" string-sort mis-ordered unpadded times ("9:30" after "10:00") → mis-sequence + idempotency drift → numeric timeToMin sort; a user-LOCKED stop past the pace cap was mis-flagged as capacity overflow → excluded (pinned = intent). (adf81f4) scheduleDay's no-slot fallback can cram a long stop past 22:00 without comfortPass pushing it → day_full guard never fired → planDay now flags late run-overs as day_full. (33dac75) preview diff compared raw time strings → cosmetic "9:00 → 09:00" no-op → compare by minute. Discover (dbdf691): dietary bias was force-injected from families ON TOP of the clearable chips so "Search without filters"/un-checking never un-filtered Eat → chips only (auto-seeded from the same profile); search race/stale-wins → monotonic reqSeq guard so only the latest loadScope writes; switching cities left the old city's results+pins on screen → clear on city change. 207 tests. ✓ device-verified: Discover opens & loads (99 Florida places, hours render, chips seeded) after the edits; travel-day engine renders on the Bal trip (1644 km long-haul tip, 227 min)
